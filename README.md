# 😁autotrolltg v1.4 (beta)
**autotrolltg** - бот для автоматизированного троллинга в Вашем профиле Telegram. Бот использует ИИ для генерации ответов, имитирует злую техподдержку компании, по-разному реагирует на грубые и вежливые ответы, помнит до 50 сообщений (настраиваемо). Почти все параметры в боте настраиваемы под Ваши предпочтения, для настройки редактируйте файл `config.json`, изменения вступят в силу после перезапуска бота.
*Разработчик бота не несёт ответственности за потенциальные дизориентацию и обиду Ваших собеседников. Используйте бота с умом!*

## СИСТЕМНЫЕ ТРЕБОВАНИЯ
**Минимальные требования (ОЧЕНЬ медленная генерация)**
- 16 гб ОЗУ
- 15 гб места на диске
- стабильное интернет-соединение

**Рекомендуемые требования**
- GTX 1060 / RTX 2060
- 16 гб ОЗУ
- 15 гб места на диске
- стабильное интернет-соединение

## УСТАНОВКА
1. Скачайте файлы из репозитория или напрямую:
```
git pull https://github.com/mrzff1/autotrolltg
```
2. Установите необходимые библиотеки:
```
pip install ollama
pip install telethon
```
3. Установите Ollama: https://ollama.com/download
4. Установите необходимые модели ИИ (по умолчанию):
```
ollama serve
```
В другом терминале выполните:
```
ollama pull deepseek-r1:8b
ollama pull tinyllama:1.1b-chat
ollama pull dolphin3:8b
```
5. Привяжите Ваш профиль. В файл `profile.json` укажите свои `API_ID` и `API_HASH` приложения. Приложение можно создать через https://my.telegram.org
6. Включите бота:
```
python3 bot.py
```
7. Авторизуйтесь. Нужно ввести номер телефона, привязанный к аккаунту и код подтверждения Telegram. *Разработчик бота не сможет завладеть Вашим аккаунтом.*
8. Готово!

## ИСПОЛЬЗОВАНИЕ
Бот **автоматически** будет отвечать на все сообщения в личные сообщения от других пользователей. Если Вы хотите, чтобы бот игнорировал кого-либо (например, Ваших знакомых), напишите ему `.whitelist` в личные сообщения. Если Вы хотите удалить пользователя из белого списка, напишите ему `.removewl`. Для выключения бота в терминале используйте `ctrl+c` вместо `ctrl+z`, иначе потребуется удаление битой базы данных `profile.session` и повторная авторизация. По умолчанию бот не реагирует на пользователей и ботов с окончанием ника `bot`, это можно отключить в параметре `ignore_bots` файла `config.json`. По умолчанию бот держит в контексте последние 50 сообщений, это можно изменить с помощью параметра `context_length` в `config.json` (большие значения будут приводить к большим затратам виртуальной памяти). Все системные промпты, ИИ-модели, параметры также изменяются в `config.json`. Бот использует строки из `faq.txt` для ответа на грубые вопросы. Строки можно изменить.

### СКРИПТОВАНИЕ
В боте присуствует система скриптования промптов через файл `tuning.json`. Для выключения скриптования переключите параметр `mercury.scripting_prompts`. Для настройки длины контекста при обработке скриптов изменяйте параметр `mercury.context_length`. Вы можете написать различные условия, которые помогут более четко подстраиваться под конкретного пользователя. Поле `condition` содержит инструкцию с условием, `trigger` - ключевое слово для определения срабатывания условия, `suffix` - приписка к системному промпту при выполнении условия.
